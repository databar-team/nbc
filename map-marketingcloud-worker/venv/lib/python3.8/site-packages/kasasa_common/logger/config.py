import os

LOG_FORMAT = '[%(asctime)s] %(levelname)-8s in %(module)s:%(lineno)d: %(message)s'

_flask_config = {
    'version': 1,
    'disable_existing_loggers': True,
    'formatters': {
        'standard': {
            'format': LOG_FORMAT
        },
    },
    'handlers': {
        'default': {
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'formatter': 'standard',
            'class': 'logging.StreamHandler',
            'stream': 'ext://sys.stdout',  # Default is stderr
        },
        'wsgi': {
            'class': 'logging.StreamHandler',
            'stream': 'ext://flask.logging.wsgi_errors_stream',
            'formatter': 'standard'
        }
    },
    'loggers': {
        '': {  # root logger
            'handlers': ['default'],
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'propagate': False
        },
        'kasasa_common': {
            'handlers': ['default'],
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'propagate': False
        },
        'api': {
            'handlers': ['wsgi'],
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'propagate': False
        }
    }
}

_no_flask = {
    'version': 1,
    'disable_existing_loggers': True,
    'formatters': {
        'standard': {
            'format': LOG_FORMAT
        },
    },
    'handlers': {
        'default': {
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'formatter': 'standard',
            'class': 'logging.StreamHandler',
            'stream': 'ext://sys.stdout',  # Default is stderr
        }
    },
    'loggers': {
        '': {  # root logger
            'handlers': ['default'],
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'propagate': False
        },
        'kasasa_common': {
            'handlers': ['default'],
            'level': os.getenv('LOG_LEVEL', 'INFO'),
            'propagate': False
        }
    }
}

try:
    import flask
    LOGGING_CONFIG = _flask_config
except ModuleNotFoundError:
    LOGGING_CONFIG = _no_flask
